import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  search: {
    width: "35%",
    marginTop: "30px",
  },
  fetchPosts: {
    marginLeft: "5%",
  },
}));

export default useStyles;
